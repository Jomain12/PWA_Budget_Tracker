document.addEventListener('DOMContentLoaded', function() {
  // nav menu
  const budget = document.querySelectorAll('.budget');
  
  // add recipe form
  const bankExchange = document.querySelectorAll('.bankExchange');
  const amount = document.querySelectorAll('.amount');

if

});